{
  "name": "auth-service",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target node",
    "dev": "bun run --hot src/index.ts"
  },
  "dependencies": {
    "hono": "^4.9.8",
    "@repo/redis": "*",
    "@repo/db": "*"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
